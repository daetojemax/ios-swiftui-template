import SwiftUI

extension Image {
    // Tab icons (using SF Symbols for template)
    public static let homeTabIcon = Image(systemName: "house.fill")
    public static let profileTabIcon = Image(systemName: "person.fill")

    // Logo
    public static let logoBlack = Image(systemName: "app.fill")
}
